# 2026-04-25 v1.0.0 Release Completion

## Scope

Complete the first public release flow for `obsidian-note-translation-companion`.

This session rebuilt the local release assets, verified manual installation in a clean Obsidian vault, ran tests and secret scans, created the `v1.0.0` GitHub Release while the repository was private, verified the release assets, then made the repository public.

## Release Assets

Verified local assets after `npm run build`:

- `main.js`
- `manifest.json`
- `styles.css`

Version checks:

- `manifest.json` version: `1.0.0`
- `versions.json`: `1.0.0` maps to `0.15.0`

## Clean-Vault Smoke Test

Created `/tmp/ntc-release-smoke-vault` with the release-layout plugin folder:

```text
.obsidian/plugins/note-translation-companion/
```

Copied `main.js`, `manifest.json`, and `styles.css` into that folder and added `English smoke note.md`.

Obsidian 1.12.7 verification:

- Opened the vault through **Open folder as vault**.
- Confirmed Obsidian detected a community plugin in the vault.
- Enabled `Note Translation Companion` from Community Plugins.
- Confirmed the installed plugin showed version `1.0.0`.
- Opened the smoke note.
- Confirmed the command palette listed `Note Translation Companion: Open Chinese Translation In New Pane`.

No provider API call was made during the smoke test.

## Automated Checks

- `npm run build`: passed.
- `npm test`: passed, 25 tests passing.
- `gitleaks git .`: passed, no leaks found.
- `gitleaks dir <HEAD snapshot>`: passed, no leaks found.
- `trufflehog --no-update git file://<repo>`: passed, zero verified or unverified secrets.
- `trufflehog --no-update filesystem <HEAD snapshot>`: passed, zero verified or unverified secrets.

TruffleHog's default updater failed before scanning because it could not move its binary, so the scans were rerun with `--no-update`.

## GitHub Release

Created `v1.0.0` while the repository was still private.

Release title:

```text
Note Translation Companion 1.0.0
```

Attached assets:

- `main.js`
- `manifest.json`
- `styles.css`

Verification after creation:

- Release is not a draft.
- Release is not a prerelease.
- Asset list contains exactly the three expected files.

## Public Publication

After release verification passed, changed `iamdevindeng/obsidian-note-translation-companion` visibility to `PUBLIC`.

Post-public checks:

- Repo visibility: `PUBLIC`.
- Default branch: `main`.
- Release URL: `https://github.com/iamdevindeng/obsidian-note-translation-companion/releases/tag/v1.0.0`.
- Release badge response resolves as `release: v1.0.0`.

## Outcome

`v1.0.0` is released publicly with the expected manual-install assets.
